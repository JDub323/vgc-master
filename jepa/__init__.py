"""JEPA world-model experiment package (pile-only, exp/jepa-world-model).

A latent one-ply world model plus a matrix-game planner for VGC doubles. See
``JEPA_DESIGN.md`` for the architecture. Nothing here is imported by the frozen
trunk agents; the round-robin protocol is the only compatibility boundary.
"""
